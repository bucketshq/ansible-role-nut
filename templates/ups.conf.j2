# {{ ansible_managed }}

{{ nut_ups_extra }}

{% for ups in nut_ups %}
[{{ ups.name }}]
    driver = {{ ups.driver }}
    port   = {{ ups.device | default('auto') }}
    desc   = "{{ ups.description | default('') }}"
    {{ ups.extra | default('') | indent(4) }}
{% endfor %}
